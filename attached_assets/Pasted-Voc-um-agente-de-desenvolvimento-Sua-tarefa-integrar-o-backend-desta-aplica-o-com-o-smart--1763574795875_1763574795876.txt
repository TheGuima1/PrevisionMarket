Você é um agente de desenvolvimento.  
Sua tarefa é integrar o backend desta aplicação com o smart contract BRL3 implantado na Polygon.  
Você deve criar todos os arquivos necessários, conectar com a blockchain, criar rotas HTTP 
para operações de mint e burn, e garantir que o painel admin consiga executar transações 
on-chain sem erros.

===========================
📌 INFORMAÇÕES DO TOKEN  
===========================
Nome do token: BRL3  
Símbolo: BRL3  
Decimais: 2  
Contrato BRL3 implantado na Polygon:  
0x0776D91e46db6e7f01815B04372570F3Ee100C9F

RPC público da Polygon (Amoy ou Mainnet):
https://polygon-rpc.com

Endereço ADMIN que possui MINTER_ROLE e PAUSER_ROLE:
0xcd83c3f36396bcb3569240a3cb34f037ba310926

O admin será o responsável por chamar mint e burn.

===========================
📌 OBJETIVO DO AGENTE
===========================
Criar TODA A INTEGRAÇÃO mint/burn na blockchain, incluindo:

1. Criar pasta: /server/blockchain/
2. Criar arquivo: /server/blockchain/contract.ts
3. Criar arquivo: /server/controllers/tokenController.ts
4. Criar arquivo: /server/routes/tokenRoutes.ts
5. Atualizar /server/routes.ts para incluir as novas rotas
6. Criar arquivo .env.example com:
   - POLYGON_RPC
   - ADMIN_PRIVATE_KEY
   - BRL3_CONTRACT
7. Configurar o backend para usar ethers.js versão 6
8. Criar funções:
   - mint(to, amount)
   - burn(amount)
9. Todas as funções devem:
   - Conectar ao provider
   - Conectar ao signer (admin)
   - Carregar contrato com ABI
   - Enviar transação
   - Retornar o tx hash
   - Tratar erros com try/catch

===========================
📌 REGRAS IMPORTANTES
===========================

1. O backend NÃO deve nunca expor a private key.
2. O amount deve ser convertido considerando DECIMALS = 2
   1 BRL3 = 100 unidades no contrato.
3. O mint SEMPRE será chamado pelo admin.
4. O burn sempre remove tokens da carteira admin.
5. Criar logs de sucesso e erro no console.
6. O retorno da rota deve ser um JSON assim:
   {
     success: true,
     hash: "0x...",
     message: "Mint realizado"
   }

===========================
📌 ARQUIVOS QUE O AGENTE DEVE CRIAR
===========================

=======================================
1️⃣ /server/blockchain/contract.ts  
=======================================
Criar arquivo que:

- importa ethers v6
- carrega provider com POLYGON_RPC
- carrega signer usando ADMIN_PRIVATE_KEY
- exporta função getContract() que retorna:
   - provider
   - signer
   - contract (instância do BRL3)
- incluir ABI completa do contrato BRL3 (copiar automaticamente do JSON compilado)

=======================================
2️⃣ /server/controllers/tokenController.ts
=======================================
Criar:

- async function adminMint(req, res)
- async function adminBurn(req, res)

Lógica de adminMint:
- ler { to, amount } do req.body
- validar se amount > 0
- converter amount * 100 (centavos)
- chamar contract.mint(to, amountConverted)
- aguardar receipt
- retornar hash

Lógica de adminBurn:
- ler { amount }
- converter amount * 100
- chamar contract.burn(amountConverted)
- aguardar receipt
- retornar hash

=======================================
3️⃣ /server/routes/tokenRoutes.ts
=======================================
Criar router com:

POST /admin/mint  → chama adminMint  
POST /admin/burn  → chama adminBurn  

=======================================
4️⃣ Atualizar /server/routes.ts
=======================================
Adicionar:

import tokenRoutes from "./routes/tokenRoutes";

router.use("/token", tokenRoutes);

=======================================
5️⃣ Criar .env.example
=======================================
Conteúdo:

POLYGON_RPC=https://polygon-rpc.com
BRL3_CONTRACT=0x0776D91e46db6e7f01815B04372570F3Ee100C9F
ADMIN_PRIVATE_KEY=COLOQUE_AQUI_MAS_NÃO_COMMITAR

=======================================
6️⃣ Ajustar package.json
=======================================
Garanta que tenha:

"dependencies": {
  "ethers": "^6.7.0"
}

Se não tiver, instale.

=======================================
7️⃣ Criar testes internos
=======================================
Adicionar um comando no final que testará as rotas:

Exemplo de curl:

curl -X POST https://SEU_BACKEND/token/admin/mint \
  -H "Content-Type: application/json" \
  -d '{"to":"0xcd83c3f36396bcb3569240a3cb34f037ba310926", "amount": 100}'

=======================================
📌 IMPORTANTE PARA O AGENTE
=======================================

- NÃO escrever código fora dos arquivos corretos.
- NÃO alterar nada do front-end.
- NÃO inventar nomes ou paths novos.
- Usar exatamente as pastas listadas.
- O backend deve iniciar sem erros.
- Validar compile-time antes de finalizar.
- Confirmar se o contrato está carregando corretamente.
- Confirmar se ethers v6 está sendo usado corretamente:
  const provider = new ethers.JsonRpcProvider(...)
  const wallet = new ethers.Wallet(PRIVATE_KEY, provider)
  const contract = new ethers.Contract(address, abi, wallet)

=======================================
📌 RESULTADO FINAL ESPERADO
=======================================

Após execução deste prompt, eu preciso ver:

✔ /server/blockchain/contract.ts criado corretamente  
✔ /server/controllers/tokenController.ts criado corretamente  
✔ /server/routes/tokenRoutes.ts criado corretamente  
✔ /server/routes.ts atualizado  
✔ .env.example criado  
✔ Backend capaz de:
    - Mintar tokens BRL3
    - Burnar tokens BRL3
✔ Transações aparecendo no Polygonscan  
✔ Nenhum erro no console  

Finalize somente quando tudo estiver funcionando.

