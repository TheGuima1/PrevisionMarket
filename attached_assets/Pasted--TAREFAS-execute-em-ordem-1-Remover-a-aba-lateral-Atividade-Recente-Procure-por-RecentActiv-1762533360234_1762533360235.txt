🛠️ TAREFAS (execute em ordem)
1) Remover a aba lateral “Atividade Recente”

Procure por RecentActivity/recent-activity-feed no front.

Remova o componente e todos os imports/uso nas páginas.

Ajuste o layout para 1 coluna fluida (cards de mercados em grid responsivo).
Arquivos prováveis:

client/src/components/recent-activity-feed.tsx → apagar

Remover referência nas páginas: home-page.tsx, market-detail-page.tsx, App.tsx (se existir)

CSS/Tailwind: se houver classes utilitárias do sidebar, ajustar container para max-w-7xl mx-auto px-4.

Critério de aceite: nenhuma referência a “Atividade Recente” e layout centralizado, sem espaço vazio lateral.

2) Rebranding (tema roxo Palpites.AI — clean/futurista)

Objetivo: paleta baseada no logo (roxos), fundo escuro suave com alto contraste, sem poluição.

2.1 Tailwind: extender tema

Edite tailwind.config.ts e adicione:

theme: {
  extend: {
    colors: {
      brand: {
        50:  '#F4EFFE',
        100: '#E9DFFE',
        200: '#CFB7FB',
        300: '#B38EF7',
        400: '#996AF2',
        500: '#7E46ED', // primária (do logo)
        600: '#6C39CB',
        700: '#5A2DA9',
        800: '#482287',
        900: '#361865',
      },
      surface: {
        DEFAULT: '#0F1220', // fundo base
        2: '#171A2A',       // cards
        3: '#1F2340',       // hover/realce
      }
    },
    boxShadow: {
      soft: '0 10px 30px rgba(0,0,0,0.25)'
    },
    borderRadius: {
      xl2: '1rem'
    }
  }
}

2.2 Aplicar nas estruturas principais

Container da app: bg-surface text-white

Cards de mercado: bg-surface-2 rounded-xl2 shadow-soft hover:bg-surface-3 transition

Botões primários: bg-brand-500 hover:bg-brand-400 text-white

Botões secundários: bg-surface-3 hover:bg-surface text-brand-200 border border-brand-700

Arquivos-alvo:

client/src/App.tsx (ou layout root)

client/src/components/navbar.tsx

client/src/components/market-card.tsx

client/src/pages/home-page.tsx

client/src/pages/market-detail-page.tsx

Critério de aceite: visual unificado roxo, fundo escuro clean, cards com sombra suave, sem “cinza genérico”.

3) “How it Works” (seção simples, acima da lista de mercados)

Nova seção explicando o fluxo PIX → BRL3 → Comprar/Vender → Resgate.

3.1 Criar componente

Crie client/src/components/how-it-works.tsx:

import React from "react";

export default function HowItWorks() {
  const Step = ({ num, title, desc }: { num: number; title: string; desc: string }) => (
    <div className="bg-surface-2 rounded-xl2 p-5 shadow-soft">
      <div className="text-brand-300 text-sm font-medium mb-1">{num}. {title}</div>
      <div className="text-white/90 text-sm leading-relaxed">{desc}</div>
    </div>
  );

  return (
    <section className="max-w-5xl mx-auto mb-8 px-4">
      <div className="text-center mb-6">
        <h2 className="text-2xl font-semibold text-white">Como funciona</h2>
        <p className="text-white/70 text-sm mt-2">Rápido, simples e 100% lastreado em BRL3.</p>
      </div>
      <div className="grid sm:grid-cols-2 lg:grid-cols-4 gap-4">
        <Step
          num={1}
          title="Deposite via PIX"
          desc="Adicione saldo com PIX. Na sequência, a plataforma emite BRL3 on-chain para sua conta."
        />
        <Step
          num={2}
          title="Escolha ‘Yes’ ou ‘No’"
          desc="Comprar ‘Yes’ ou ‘No’ é como apostar no resultado. As probabilidades mudam em tempo real conforme outros negociam."
        />
        <Step
          num={3}
          title="Negocie sem taxas"
          desc="Coloque ordens de compra/venda a qualquer momento. Sem limites de aposta e sem taxas de trade."
        />
        <Step
          num={4}
          title="Resgate 1 BRL3 por cota vencedora"
          desc="Quando o mercado encerra, cada cota vencedora vale 1 BRL3. Você também pode vender suas cotas antes do fim."
        />
      </div>
    </section>
  );
}

3.2 Usar no Home

Edite client/src/pages/home-page.tsx:

Importe HowItWorks e renderize acima da grade de mercados.

Adicione um CTA no topo (hero curto) com o logo e botão “Depositar via PIX”.

Hero exemplo (opcional):

<section className="max-w-5xl mx-auto px-4 mt-6 mb-6 text-center">
  <h1 className="text-3xl md:text-4xl font-bold text-white">Acerte previsões. Ganhe em BRL3.</h1>
  <p className="text-white/70 mt-2">Probabilidade = preço. Cada cota vencedora paga 1 BRL3.</p>
  <div className="mt-4 flex justify-center gap-3">
    <a href="/wallet/deposit" className="px-4 py-2 bg-brand-500 hover:bg-brand-400 rounded-xl2">Depositar via PIX</a>
    <a href="/markets" className="px-4 py-2 bg-surface-3 hover:bg-surface rounded-xl2">Explorar mercados</a>
  </div>
</section>
<HowItWorks />


Critério de aceite: seção “Como funciona” visível na Home com 4 passos claros (PIX, Comprar, Negociar, Resgatar).

4) Ajustes de cópia e labels (BRL3 + PIX)

Trocar “USD/USDC” por BRL3 em toda a UI (saldo, ordens, histórico).

Adicionar botões visíveis no detalhe do mercado:

Criar Par: “1 BRL3 → 1 YES + 1 NO”

Resgatar Par: “1 YES + 1 NO → 1 BRL3”

Na área de compra/venda, exibir trio: Prob% · Odds (x) · Preço (BRL3).

Arquivos-alvo:

client/src/pages/market-detail-page.tsx

client/src/components/trade-panel.tsx

client/src/components/odds-display.tsx

Componentes de carteira/transações.

Critério de aceite: nenhuma menção a USD/USDC; BRL3 padrão; botões Criar/Resgatar visíveis.

5) Botões/fluxo PIX e on-chain (front)

Criar página simples /wallet/deposit explicando:

“Pague com PIX → saldo é tokenizado em BRL3 on-chain na sua conta”.

Botão “Gerar QR PIX” (chama endpoint existente/futuro).

Adicionar call-to-action “Depositar via PIX” na Home e no Navbar.

Arquivos-alvo:

client/src/pages/deposit-page.tsx (novo)

client/src/components/navbar.tsx (link)

Critério de aceite: usuário encontra o botão PIX em 1 clique a partir da Home/Nav.

6) Pequenos refinamentos visuais

Cards de mercado:

Mostrar Prob% grande, e abaixo “Odds (x)” e “Preço (BRL3)”.

Badges “YES/NO” minimalistas: bg-brand-700 text-brand-100 rounded px-2 py-0.5.

Tabela/linhas: divide-y divide-white/5.

Inputs: bg-surface-3 border border-white/10 focus:border-brand-500.

Arquivos-alvo:

client/src/components/market-card.tsx

client/src/components/order-book.tsx

client/src/components/trade-panel.tsx

Critério de aceite: leitura rápida do principal (Prob%, Odds, Preço), sem poluição.

🧪 CHECKLIST FINAL (aceite)

 Atividade Recente removida e layout centralizado (sem espaços inúteis).

 Tema roxo Palpites.AI aplicado (cores, botões, cards).

 Home com Hero + “Como funciona” (4 passos).

 BRL3/PIX em toda a UI (sem USDC).

 Market Detail mostra Prob% · Odds (x) · Preço (BRL3) e botões Criar Par / Resgatar Par.

 Navegação simples para Depositar via PIX.

 Nenhum erro de build/lint; layout responsivo.

🔁 MENSAGENS DE COMMIT (sugestão)

feat(ui): add HowItWorks section with PIX→BRL3 flow

chore: remove RecentActivity panel and unused imports

feat(theme): apply Palpites.AI purple branding + clean surfaces

feat(market): show Prob% · Odds(x) · Price(BRL3) and mint/redeem buttons

feat(wallet): add deposit via PIX entry point